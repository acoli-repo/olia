# nominalizing suffixes that do not allow for successive verbablization (converbs), may only be followed by possessive and amb. suffixes

define VtoNfinal [
  "+DS" : [{pti}"@PTI"|{qti}]
| "+SS_Sim" : [{sti}"@STIN"|{stin}]
| "+SS" : [{spa}|{shpa}]]
;
